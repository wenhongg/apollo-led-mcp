{
  "mcpServers": {
    "apollo-led": {
      "type": "stdio",
      "command": "bash",
      "args": ["-c", "cd /home/openclaw_user/apollo-led-mcp && exec venv/bin/python -m src.mcp_server"],
      "env": {
        "APOLLO_URL": "http://localhost:9092"
      }
    }
  }
}
